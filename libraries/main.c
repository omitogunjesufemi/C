#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
	if (_islower('a'))
		printf("True\n");
	else
		printf("False\n");
	return (0);
}
